<h1>Your Notes</h1>
{{#each notes}}
<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <div class="card">
      <div class="card-body">
        <h4>{{title}}</h4>
        <p>{{details}}</p>
      </div>
    </div>
  </div>
</div>
{{else}}
<p>You don't have any notes right now.</p>
{{/each}}